/**
 * This is replaced after build with the built files outputted as a workbox precache compliant array.
 */
declare const SERVICE_WORKER_MANIFEST_ENTRIES: Array<{
  url: string;
  revision?: string | null;
}>;
